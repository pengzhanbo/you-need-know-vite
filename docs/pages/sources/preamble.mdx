---
title: 序言
---

import Mermaid from '@components/Mermaid'

<Mermaid chart={`
flowchart TD
  CS(create server) -->|connect| M[middlewares]
  RC(resolve config) --> CS
  M --> HS[http server]
  CS -->|WebSocket| WS[ws]
  CS -->|chokidar| FW[watcher]
  CS --> MG[module graph]
  MG --> PC
  FW --> PC[Plugin container]
  WS ---> S([vite dev server])
  M --> S
  HS --> S
  MG --> S
  PC --> S
  FW --> S
`}
/>
