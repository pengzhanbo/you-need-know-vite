---
title: 序言
---

import Mermaid from '@components/Mermaid'


## createServer

<Mermaid chart={`
flowchart TD
  CS(create server) --> M[middlewares]
  RC(resolve config) --> CS
  M --> HS[http server]
  CS --> WS[ws]
  CS --> FW[watcher]
  CS --> MG[module graph]
  MG --> PC
  FW --> PC[plugin container]
  WS ----> S([vite dev server])
  M ---> S
  HS ---> S
  MG ---> S
  PC ---> S
  FW ---> S
`}
/>

## build

<Mermaid chart={`
flowchart TD
  RC(resolve config) --> RS
  RS(resolve rollup option) --> R[rollup bundler]

`}
/>

## preview

<Mermaid chart={`
flowchart TD
  RC(resolve config) --> HS(http server)

`}
/>
